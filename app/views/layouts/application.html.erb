<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= content_for(:title) || "遺品AI査定" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="遺品の写真を撮るだけでAIが査定。遺族に寄り添う遺品管理サービスです。">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="いひん">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/navbar" %>

    <% if notice %>
      <div class="flash flash-notice" data-controller="flash">
        <span><%= notice %></span>
        <button data-action="flash#close" class="flash-close">&times;</button>
      </div>
    <% end %>
    <% if alert %>
      <div class="flash flash-alert" data-controller="flash">
        <span><%= alert %></span>
        <button data-action="flash#close" class="flash-close">&times;</button>
      </div>
    <% end %>

    <main class="main-content">
      <%= yield %>
    </main>

    <%= render "shared/footer" %>
    <%= render "shared/dev_bar" %>
  </body>
</html>
