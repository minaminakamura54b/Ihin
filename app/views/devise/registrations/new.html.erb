<% content_for :title, "新規登録 | いひん" %>

<div class="auth-container">
  <div class="auth-card">
    <div class="auth-header">
      <span class="brand-icon">🏮</span>
      <h1>無料で登録する</h1>
      <p>遺族・家族は完全無料でご利用いただけます</p>
    </div>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
        data: { controller: "registration" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label :name, "お名前", class: "form-label" %>
        <%= f.text_field :name, autofocus: true, class: "form-input", placeholder: "例：山田 太郎" %>
      </div>

      <div class="form-group">
        <%= f.label :email, "メールアドレス", class: "form-label" %>
        <%= f.email_field :email, autocomplete: "email", class: "form-input", placeholder: "example@email.com" %>
      </div>

      <div class="form-group">
        <%= f.label :password, "パスワード", class: "form-label" %>
        <p class="form-hint"><%= @minimum_password_length %>文字以上</p>
        <%= f.password_field :password, autocomplete: "new-password", class: "form-input" %>
      </div>

      <div class="form-group">
        <%= f.label :password_confirmation, "パスワード（確認）", class: "form-label" %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-input" %>
      </div>

      <div class="form-group">
        <%= f.label :role, "ご利用目的", class: "form-label" %>
        <%= f.select :role, [["遺族・家族として利用（無料）", "family"],
            ["業者として登録（有料）", "business"]],
            { selected: params[:role] || "family" },
            class: "form-select",
            data: { action: "change->registration#toggleAddress" } %>
      </div>

      <%# 遺族向けエリア入力（業者選択時は非表示） %>
      <div data-registration-target="addressSection">
        <div class="form-group">
          <%= f.label :prefecture, "都道府県", class: "form-label" %>
          <%= f.text_field :prefecture, class: "form-input", placeholder: "例：東京都" %>
        </div>
        <div class="form-group">
          <%= f.label :city, "市区町村", class: "form-label" %>
          <%= f.text_field :city, class: "form-input", placeholder: "例：渋谷区" %>
        </div>
      </div>

      <div class="form-actions">
        <%= f.submit "登録する", class: "btn btn-primary btn-full", data: { disable_with: "登録中..." } %>
      </div>
    <% end %>

    <div class="auth-links">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
